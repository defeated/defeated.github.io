{
  "scripts": {
    "setup": "bundle && npm install",
    "build": "gulp build",
    "start": "gulp"
  },

  "devDependencies": {
    "autoprefixer-core": "^5.1.8",
    "browser-sync": "^2.5.3",
    "gulp": "^3.8.11",
    "gulp-babel": "^5.1.0",
    "gulp-minify-css": "^1.0.0",
    "gulp-postcss": "^5.1.0",
    "gulp-rename": "^1.2.2",
    "gulp-sass": "^1.3.3",
    "gulp-uglify": "^1.2.0"
  }
}
